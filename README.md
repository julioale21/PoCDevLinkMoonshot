
## Getting Started

First, run the development server:

1 -  Clone the repository

2 - Run the command
    ```
        npm install
    ```
3 - Add .env variables

4 - Run the command
    ```
        npm run dev
    ```
